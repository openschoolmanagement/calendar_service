import { Request } from "express";
import { Command } from "../../controller/commands/Command";

export class CommandConverter {
  //   static convert(request: Request): Command[] {
  //     const result: Command[] = CommandConverter.parseBody(request.body);
  //     return result;
  //   }
  //   private static parseBody(requestBody:  ): Command[] {
  //   }
}
